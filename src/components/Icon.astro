---
import { Icon as AstroIcon } from "astro-icon/components";

/**
 *  Icon sets and names: https://icon-sets.iconify.design/
 */
interface Props {
  name: string;
  twColor?: string;
}

const { name, twColor } = Astro.props;
---

<AstroIcon
  name={`material-symbols-light:${name}`}
  size={50}
  class={twColor ?? "text-slate-400"}
/>
